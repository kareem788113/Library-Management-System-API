# Library-Management-System-API
A RESTful API for a library system with JWT authentication and role-based access control. Users can browse, borrow, and return books with validation for availability and limits. Built with a clean modular architecture for scalability and maintainability.
